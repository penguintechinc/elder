# Ensure optional dependencies are installed (required for multi-arch Docker builds)
# Native packages like @rollup/rollup-*, @esbuild/*, and lightningcss-* need this
optional=true

# Prefer offline packages when available (speeds up CI builds)
prefer-offline=true

# Don't save exact versions (use semver ranges from package.json)
save-exact=false

# GitHub Packages registry for @penguintechinc scope
@penguintechinc:registry=https://npm.pkg.github.com
# Auth token should be set in environment: //npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
